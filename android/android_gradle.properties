# Disable Kotlin incremental compilation to avoid cross-drive incremental cache errors on CI
kotlin.incremental=false

# Optional: run Kotlin compiler in-process to avoid daemon/remote issues
kotlin.compiler.execution.strategy=in-process